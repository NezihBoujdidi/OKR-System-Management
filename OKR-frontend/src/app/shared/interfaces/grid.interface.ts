export interface GridConfig {
  data: any[];
  imageKey: string;
  titleKey: string;
  subtitleKey: string;
  badgeKey: string;
  teamKey: string;
  itemsPerPage?: number;
  enablePagination?: boolean;
} 